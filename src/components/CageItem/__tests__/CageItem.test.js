/* eslint-env jest */

import React from 'react'
import { shallow } from 'enzyme'

import CageItem from '../index'

describe('CageItem', () => {
  it('render component', () => {
    //  const component = shallow(<CageItem />)
    //  expect(component).toMatchSnapshot()
  })
})
